services:
  - type: web
    name: riskgraph
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn riskgraph.api.server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
